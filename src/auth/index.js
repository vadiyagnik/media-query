import React from 'react';

export const Registration = () => {
    return (
        <div>Registration Form</div>
    );
};

export const Login = () => {
    return (
        <div>Login Form</div>
    );
};